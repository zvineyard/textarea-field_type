<textarea
    class="form-control"
    name="{{ field_type.nagetFieldName() }}"
    rows="{{ field_type.config.rows ?: 4 }}"
    placeholder="{{ field_type.placeholder }}"
    >{{ value }}</textarea>
